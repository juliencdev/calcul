{% extends 'base.html.twig' %}

{% block title %}Cadeau{% endblock %}

{% block body %}
    <h1>Cadeau</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cadeau.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ cadeau.nom }}</td>
            </tr>
            <tr>
                <th>AgeParfait</th>
                <td>{{ cadeau.ageParfait }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('cadeau_index') }}">back to list</a>

    <a href="{{ path('cadeau_edit', {'id': cadeau.id}) }}">edit</a>

    {{ include('cadeau/_delete_form.html.twig') }}
{% endblock %}
